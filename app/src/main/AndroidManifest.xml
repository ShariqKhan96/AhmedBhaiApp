<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.ahmedbhaiapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AllAtivities.MainActivity"></activity>
        <activity android:name=".AllAtivities.Recommended" />
        <activity android:name=".AllAtivities.Login"></activity>
        <activity
            android:name=".AllAtivities.Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".AllAtivities.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AllAtivities.Dummy"
            android:label="@string/title_activity_dummy"
            android:theme="@style/AppTheme">

        </activity>
    </application>

</manifest>